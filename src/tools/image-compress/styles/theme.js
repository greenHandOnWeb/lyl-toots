import { PALETTE } from '@/styles/palette.js'

/** 图片压缩工具 — 使用全局 Emerald 绿色色板 */
export const IC_THEME = {
  accent: PALETTE.primary,
  success: PALETTE.success,
  danger: PALETTE.danger,
  track: PALETTE.track,
}
